Drop the final brand assets here before deployment:
- logo.png     Used in the navigation, footer, docs, and admin branding.
- favicon.png  Used as the site favicon / app icon.

The interface has a text fallback for logo.png so the app still renders before the final asset is uploaded.

'use client';
import { Nav } from '@/components/Nav';
import { Footer } from '@/components/Footer';
import { useAccount, useSignMessage } from 'wagmi';
import { useMemo, useState } from 'react';
import { topBrands } from '@/lib/demo';
import { ArrowUpRight, Upload } from 'lucide-react';

export default function ListWatch(){
 const {address}=useAccount(); const {signMessageAsync}=useSignMessage(); const [code,setCode]=useState(''); const [status,setStatus]=useState('');
 const [form,setForm]=useState({brand:'Rolex',model:'',reference_number:'',year:'2021',condition:'Excellent',appraised_value_usd:'',tokenized_percent:'25',description:'',box_papers:true,serial_verified:false,primary_image_url:'',verification_image_url:''});
 const slug=useMemo(()=>`${form.brand}-${form.model}-${form.reference_number}`.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,''),[form]);
 async function generateCode(){const r=await fetch('/api/verification-code',{method:'POST'}).then(r=>r.json());setCode(r.code)}
 async function upload(kind:string, file?: File){ if(!file) return; setStatus(`Uploading ${kind}...`); const data=new FormData(); data.append('file',file); data.append('path',`${kind}-${Date.now()}`); const r=await fetch('/api/upload',{method:'POST',body:data}); const j=await r.json(); if(!r.ok){setStatus(j.error||'Upload failed'); return;} setForm(f=>({...f,[kind==='primary'?'primary_image_url':'verification_image_url']:j.url})); setStatus(`${kind} image uploaded.`);}
 async function submit(){ if(!address) return setStatus('Connect wallet first.'); if(!code) return setStatus('Generate your verification code first.'); const message=`Timepiece listing verification\nWallet: ${address}\nCode: ${code}\nWatch: ${form.brand} ${form.model} ${form.reference_number}`; const signature=await signMessageAsync({message}); const payload={...form,slug,owner_wallet:address,verification_code:code,signature,year:Number(form.year),appraised_value_usd:Number(form.appraised_value_usd),tokenized_percent:Number(form.tokenized_percent),verification_status:'submitted',published:false}; const res=await fetch('/api/watches',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}); const json=await res.json(); setStatus(res.ok?'Submitted. Admin can now verify and publish it.':json.error||'Error'); }
 return <main className="page"><Nav/><section className="section" style={{paddingTop:150}}><div className="container split" style={{alignItems:'start'}}><div><div className="kicker">List your watch</div><h1 className="h2 serif">Turn a verified watch into a market.</h1><p className="lead">Connect your wallet, enter the watch details, then prove possession with a code photo. After review, your watch can be published and linked to a Pons token.</p><div className="panel" style={{padding:28,marginTop:28}}><h3>Possession code</h3><p style={{color:'var(--muted)'}}>Write this code on paper. Place it beside your watch and upload a photo where both are clear.</p><div style={{fontSize:42,letterSpacing:'.14em',color:'var(--green)',fontWeight:900}}>{code||'— — —'}</div><button className="pill" onClick={generateCode}>Generate code</button></div></div><div className="panel" style={{padding:28}}><div className="form"><label className="label">Brand<select className="select" value={form.brand} onChange={e=>setForm({...form,brand:e.target.value})}>{topBrands.map(b=><option key={b}>{b}</option>)}</select></label><label className="label">Model<input className="input" value={form.model} onChange={e=>setForm({...form,model:e.target.value})}/></label><label className="label">Reference<input className="input" value={form.reference_number} onChange={e=>setForm({...form,reference_number:e.target.value})}/></label><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:16}}><label className="label">Year<input className="input" value={form.year} onChange={e=>setForm({...form,year:e.target.value})}/></label><label className="label">Condition<select className="select" value={form.condition} onChange={e=>setForm({...form,condition:e.target.value})}>{['Unworn','Excellent','Very Good','Good','Fair'].map(x=><option key={x}>{x}</option>)}</select></label></div><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:16}}><label className="label">Appraised value USD<input className="input" value={form.appraised_value_usd} onChange={e=>setForm({...form,appraised_value_usd:e.target.value})}/></label><label className="label">Tokenized %<input className="input" value={form.tokenized_percent} onChange={e=>setForm({...form,tokenized_percent:e.target.value})}/></label></div><label className="label">Primary watch photo<input className="input" type="file" accept="image/*" onChange={e=>upload('primary',e.target.files?.[0])}/>{form.primary_image_url&&<span>Uploaded ✓</span>}</label><label className="label">Code verification photo<input className="input" type="file" accept="image/*" onChange={e=>upload('verification',e.target.files?.[0])}/>{form.verification_image_url&&<span>Uploaded ✓</span>}</label><label className="label">Description<textarea className="textarea" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><button className="pill green" onClick={submit}>Submit for verification <ArrowUpRight size={16}/></button>{status&&<p style={{color:'var(--green)'}}>{status}</p>}</div></div></div></section><Footer/></main>
}
